@model decimal?
@Html.DevExpress().CallbackPanel(settings =>
{
    settings.Name = "cbpHistoryWrapupContainer";
    settings.CallbackRouteValues = new { Controller = "Crm", Action = "IndexCallbackPanelHistoryWrapupPartial" };
    settings.ClientSideEvents.BeginCallback = settings.Name + "_BeginCallback";
    settings.ClientSideEvents.EndCallback = settings.Name + "_EndCallback";
    settings.Width = Unit.Percentage(100);
    settings.SetContent(() =>
    {
        Html.RenderAction("IndexHistoryWrapupPartial", new { _clientId = Model });    
    });
}).GetHtml()